# AGENTS.md

## 项目概述

该项目为一个第三方QQ机器人, 用于转发金融相关新闻和数据。

## 开发环境

- Node: v24.18.0
- 使用 npm 进行包管理

## 远程调试

- 可选服务器:
    - SSH 主机别名：`qq-bot-server`
    - 登录用户：`ubuntu`
    - 项目目录：`/home/ubuntu/qq-market-bot`
    - 项目使用 Docker 进行部署运行
    - 密钥认证由本机 SSH 配置管理

- 在远程服务器中拉取 Github 代码请使用 https://ghfast.top/ 作为前缀加速

## 项目结构

- `src\`: 项目程序代码
- `docs\`: 文档
- `tests\`: 测试

## 编码规范

- 编码需要统一现有风格
- 命名清晰，变量、函数使用完整且有意义的名称
- 充分利用类型系统，尽量避免 `any`
- 函数保持短小、单一职责
- 避免重复代码
- 模块职责清晰，避免依赖循环

## 测试

- 项目中添加新功能时必须完成对应的测试
- Bug 修复必须添加回归测试
- 测试后请删除测试时所产生的缓存文件

## 完成标准

任务完成前请确认:

1. 测试通过
2. 相关文档已更新

## 注意事项

- 不要无故升级或更新依赖
- 不要修改生成文件
- 不要破坏现有公共 API

## 行情数据源

- 项目的行情数据接入统一使用同花顺金融数据 API。
- 同花顺 API 文档索引：<https://fuyao.aicubes.cn/llms.txt>。
- 同花顺 API 完整接口文档：<https://fuyao.aicubes.cn/llms-full.txt>。涉及行情数据的设计、实现或测试前，先读取与任务相关的章节；以完整接口文档为准。
- 使用 REST API 接入，认证信息通过请求头 `X-api-key` 提供。API Key 属于敏感信息，只能存放在本地环境变量或部署环境的密钥管理中，不得写入源码、测试夹具、日志、示例文件或提交到仓库。

## QQBot开发文档

- NapcatQQ: https://napneko.github.io/guide/start-install
